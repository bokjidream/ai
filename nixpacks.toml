[phases.setup]
nixPkgs = ["nodejs_20", "cacert", "uv"]

[phases.install]
cmds = [
  "uv sync --frozen --no-dev",
  "npm install --prefix ./scripts --omit=dev"
]

[start]
cmd = "uv run uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}"
